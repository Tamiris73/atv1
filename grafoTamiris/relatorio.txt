Este relatório apresenta um software com funções para ler arquivos “.txt” e interpretá-los como uma matriz numpy. O software utiliza a biblioteca numpy do Python para realizar essa tarefa. Primeiramente, o código importa a biblioteca numpy e lê o arquivo de texto usando a função interna open() do Python. Em seguida, ele utiliza a função genfromtxt() do numpy para carregar os dados em uma matriz numpy.
Essa função irá ler o conteúdo da instância e armazená-lo em um array numpy chamado dados. 
Em seguida é executada a função resultado:
A função recebe dois parâmetros: instância, que corresponde ao nome da instância passado na execução do código, e matriz, que corresponde a tudo a matriz criada pela função criaMatriz. A função imprime o nome da instância e as dimensões da matriz na tela, usando a função print. A função também escreve as dimensões da matriz em um arquivo chamado resultados.txt, localizado no caminho especificado pela variável path. A função usa o método shape do objeto matriz para obter o número de linhas e colunas. A função não retorna nenhum valor.
O código a seguir consciente na função main, o corpo principal do programa e que chama a biblioteca criada data para criar e exibir uma matriz a partir de um arquivo de entrada.
O código usa o módulo sys para obter o nome do arquivo de entrada como um argumento da linha de comando. Em seguida, ele chama a função criaMatriz da classe entradaSaida para criar uma matriz a partir do arquivo. Por fim, o código chama a função saidaResultado da mesma classe para exibir a matriz na tela. O código usa a condição if __name__ == '__main__' para garantir que o script seja executado apenas quando for chamado diretamente e para puxar o nome da instância desejada.
